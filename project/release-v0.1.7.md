# v0.1.7

OpenClaw-specific integration removal before launch.

- Removed the workspace OpenClaw configuration panel and sync endpoints
- Removed stored OpenClaw integration data from the schema
- Replaced the old OpenClaw-specific follow-up list with a single spec-pending external agent registry item
