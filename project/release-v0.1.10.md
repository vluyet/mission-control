# v0.1.10

Docker-native OpenClaw proxying for Mission Control.

- Added an `openclaw-proxy` service to the production compose stack
- Updated Docker deployment guidance to use `http://openclaw-proxy:18790` for Mission Control OpenClaw linkage
