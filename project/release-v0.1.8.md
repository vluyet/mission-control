# v0.1.8

OpenClaw MVP integration return.

- Added workspace-level OpenClaw gateway configuration and agent sync
- Added OpenClaw-backed task dispatch from Mission Control
- Added agent-facing reporting conventions so OpenClaw runs use Mission Control APIs instead of UI scraping
