# v0.1.6

OpenClaw binding documentation and operator UX follow-up.

- Added a dedicated OpenClaw binding guide for Mission Control operators
- Clarified that dashboard URL is reference metadata only and does not perform discovery
- Improved the `spawn openclaw ENOENT` error into a clearer CLI-not-found setup message
- Logged next-step backlog items for JSON5-safe parsing and OpenClaw setup diagnostics
